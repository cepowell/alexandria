.login-box
    .box-header
        %h2 Forgot Password?
    =form_for(:password_reset, url: password_resets_path) do |f|
        =f.label :email
        %br
        =f.email_field :email
        %br
        =f.submit "Request Reset", class: "button small"